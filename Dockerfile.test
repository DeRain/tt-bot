FROM golang:1.26-alpine

RUN apk add --no-cache gcc musl-dev

WORKDIR /app
COPY go.mod go.sum* ./
RUN go mod download 2>/dev/null || true
COPY . .

CMD ["go", "test", "./...", "-tags=integration", "-run", "Integration|E2E", "-v", "-count=1"]
